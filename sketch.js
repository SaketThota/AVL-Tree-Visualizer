function setup() { 
    let canvas = createCanvas( window.innerWidth - document.querySelector(".tools-wrapper").offsetWidth, window.innerHeight);   

    canvas.parent('canvas');
    canvas.style('display', 'block');
    background(100);
}

function draw() { 
    
}